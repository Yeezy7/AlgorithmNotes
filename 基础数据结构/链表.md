``` c++
typedef struct LNode{
    int data;
    struct LNode *next;
}LNode, *LinkList;

int main{
    # 定义链表
    LinkList head = new LNode;
    head->next = NULL;
    
    # 定义结点
    LNode *node = new LNode;
    p->data = num;
    head->next=p;
}
```